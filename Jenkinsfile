pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                                // Print Java and Maven version for debugging
                sh 'java -version'
                sh 'mvn -version'
            }
        }
    }
}
